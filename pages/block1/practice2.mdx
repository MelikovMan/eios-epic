# Практическое задание для урока 2
## Задание 1: Базовый JSX
Создайте компонент, который отображает:

Заголовок с вашим именем

Список ваших хобби

Текущую дату

Случайное число от 1 до 100

``` jsx
function AboutMe() {
  // Ваш код здесь
}
```
## Задание 2: Компонент погоды
Создайте компонент погоды, который принимает пропсы:

temperature (температура)

city (город)

isSunny (солнечно ли)

```jsx
function WeatherCard({ temperature, city, isSunny }) {
  // Ваш код здесь
}
```
## Задание 3: Список задач
Создайте компонент списка задач с условным рендерингом:

Если задач нет - показать сообщение

Если задачи есть - показать список


## Задание 4: Компонент карточки пользователя
Создайте компонент UserCard, который принимает props:

user (объект с name, email, avatar)

onMessage (функция)

isOnline (булево значение)

## Задание 5: Компонент модального окна
Создайте компонент Modal, который принимает:

isOpen (открыто/закрыто)

onClose (функция закрытия)

title (заголовок)

children (содержимое)

## Задание 6: Компонент списка с фильтрацией
Создайте компонент FilterableList, который:

Принимает массив элементов

Отображает поле поиска

Фильтрует элементы по введенному тексту

Использует подъем состояния для фильтрации

Указанные компоненты должны быть на единой веб-странице с возможностью перехода по якорю.